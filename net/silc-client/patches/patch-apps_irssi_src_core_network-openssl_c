Index: apps/irssi/src/core/network-openssl.c
--- apps/irssi/src/core/network-openssl.c.orig
+++ apps/irssi/src/core/network-openssl.c
@@ -82,7 +82,7 @@ static const char *tls_dns_name(const GENERAL_NAME * g
 	}
 
 	/* Safe to treat as an ASCII string possibly holding a DNS name */
-	dnsname = (char *) ASN1_STRING_data(gn->d.ia5);
+	dnsname = (const char *) ASN1_STRING_get0_data(gn->d.ia5);
 
 	if (has_internal_nul(dnsname, ASN1_STRING_length(gn->d.ia5))) {
 		g_warning("Internal NUL in subjectAltName");
